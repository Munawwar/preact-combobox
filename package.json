{
  "name": "react-multiselect-autocomplete",
  "version": "0.0.1",
  "description": "Multiselect autocomplete component for react and preact projects",
  "main": "dist/cjs/MultiSelectAutocomplete.js",
  "module": "dist/esm/MultiSelectAutocomplete.js",
  "scripts": {
    "prepare": "npm run build",
    "prepublish": "npm run build",
    "lint": "npx @biomejs/biome check --write",
    "pretest": "npm run build",
    "example": "browser-sync start --server --files 'example' --files 'dist' --open './example/example-react.html'",
    "watch": "esbuild ./lib/*.jsx --outdir=dist/esm --format=esm --bundle --sourcemap --external:react --watch",
    "build": "rm -fr dist && esbuild ./lib/*.jsx --outdir=dist/esm --format=esm --bundle --sourcemap --external:react && esbuild ./lib/*.jsx --outdir=dist/cjs --format=cjs --bundle --sourcemap --external:react"
  },
  "keywords": ["react", "preact", "tags", "tag input", "component", "multiselect", "autocomplete"],
  "author": "Munawwar",
  "license": "MIT",
  "repository": "https://github.com/Munawwar/preact-tag-autocomplete",
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "browser-sync": "^3.0.2",
    "esbuild": "^0.21.5",
    "typescript": "^5.5.3"
  },
  "engines": {
    "node": ">= 10.0.0"
  }
}
